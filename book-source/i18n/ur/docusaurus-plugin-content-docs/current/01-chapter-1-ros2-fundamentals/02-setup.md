---
title: "اپنا ROS 2 ماحول ترتیب دینا"
chapter: 1
lesson: 2
duration_minutes: 45

# Robotics-Specific Metadata
simulation_required: ["turtlesim"]
safety_level: "simulation_only"
cefr_level: "A2"
hardware_prerequisites: []

# Learning Objectives (Measured, SMART)
learning_objectives:
  - "Install ROS 2 Humble on Ubuntu 22.04"
  - "Verify ROS 2 installation with CLI tools"
  - "Launch Turtlesim simulator and control a virtual robot"
  - "Set up Python development environment for ROS 2"
  - "Understand environment variables (ROS_DOMAIN_ID, ROS_DISTRO)"

# Pedagogical Layer
primary_layer: "Layer 1 (Manual Foundation)"
---

<PersonalizedLesson lessonPath="01-chapter-1-ros2-fundamentals/02-setup.md">

## تعارف (Introduction)

اس سے پہلے کہ آپ ROS 2 کوڈ لکھ سکیں، آپ کو ایک کام کرنے والے ترقیاتی ماحول (development environment) کی ضرورت ہے۔ یہ سبق آپ کو ROS 2 Humble—جو کہ موجودہ long-term support (LTS) ورژن ہے—انسٹال کرنے اور Turtlesim، جو آپ کا پہلا روبوٹ سمیلیٹر ہے، کو لانچ کرنے کے عمل سے گزارے گا۔

**متوقع وقت**: 45 منٹ (مستحکم انٹرنیٹ اور بغیر کسی غلطی کے فرض کرتے ہوئے)

**ضروریات (Prerequisites)**:
- Ubuntu 22.04 LTS والا کمپیوٹر یا VM/WSL2 کے ساتھ Windows/macOS
- 3GB خالی ڈسک اسپیس
- انٹرنیٹ کنکشن
- ٹرمینل سے واقفیت (cd, ls, sudo کمانڈز)

---

## اس سبق میں ہم کیا کریں گے

اس سبق کے اختتام تک، آپ کے پاس ہوگا:

1. ✓ ROS 2 Humble انسٹال اور تصدیق شدہ
2. ✓ Turtlesim ایک سیمولیشن ونڈو میں چل رہا ہے
3. ✓ ماحولیاتی متغیرات (Environment variables) مناسب طریقے سے ترتیب دیئے گئے
4. ✓ Python 3.10+ جو ROS 2 ڈیویلپمنٹ کے لیے تیار ہے
5. ✓ سبق 3 میں نوڈس (nodes) لکھنے کا اعتماد

---

## تصور 1: Linux کی ضروریات اور ماحول

ROS 2 سرکاری طور پر **Ubuntu 22.04 LTS** (Jammy Jellyfish) کو ٹارگٹ کرتا ہے۔ اگرچہ ROS 2 دیگر Linux ڈسٹری بیوشنز پر بھی چلتا ہے، لیکن Ubuntu سب سے بہترین سپورٹڈ پلیٹ فارم ہے۔

### اگر آپ کے پاس Ubuntu 22.04 نہیں ہے

**آپشن 1: Native Ubuntu**
- https://ubuntu.com/download/desktop سے Ubuntu 22.04 LTS ڈاؤن لوڈ کریں
- کسی فالتو کمپیوٹر پر انسٹال کریں یا اپنے OS کو تبدیل کریں

**آپشن 2: Virtual Machine (Windows/macOS)**
- VirtualBox (مفت) ڈاؤن لوڈ کریں: https://www.virtualbox.org/
- Ubuntu 22.04 کے ساتھ ایک VM بنائیں
- کم از کم 2 CPU cores اور 4GB RAM مختص کریں
- ٹیوٹوریل: https://www.virtualbox.org/wiki/Documentation

**آپشن 3: WSL2 (صرف Windows)**
- Windows Subsystem for Linux 2 آپ کو Windows کے اندر Ubuntu چلانے کی اجازت دیتا ہے
- سیٹ اپ: https://docs.microsoft.com/en-us/windows/wsl/install
- انسٹال کرنے کے بعد، چلائیں: `wsl --install -d Ubuntu-22.04`

**آپشن 4: Docker Container (Advanced)**
- پہلے سے تیار شدہ ROS 2 Docker امیجز دستیاب ہیں
- تجربہ کار ڈیویلپرز کے لیے تجویز کردہ
- دستاویزات: https://hub.docker.com/r/osrf/ros

---

## مرحلہ 1: اپنے سسٹم کو اپ ڈیٹ کریں

ایک ٹرمینل کھولیں اور Ubuntu کے پیکیج مینیجر کو اپ ڈیٹ کریں:

```bash
sudo apt update
sudo apt upgrade -y
````

یہ تازہ ترین پیکیج لسٹس ڈاؤن لوڈ کرتا ہے اور موجودہ سافٹ ویئر کو اپ گریڈ کرتا ہے۔ آپ سے پاس ورڈ مانگا جا سکتا ہے (یہ معمول کی بات ہے)۔

**متوقع آؤٹ پٹ (Expected output)**:

```text
Reading package lists... Done
Building dependency tree... Done
The following packages will be upgraded:
  curl git vim [... more packages ...]
Processing triggers for [... more output ...]
```

-----

## مرحلہ 2: ROS 2 ریپوزٹری شامل کریں

ROS 2 Ubuntu کی ڈیفالٹ ریپوزٹریز میں نہیں ہے۔ سرکاری ROS 2 ریپوزٹری شامل کریں:

```bash
sudo curl -sSL [https://raw.githubusercontent.com/ros/ros.key](https://raw.githubusercontent.com/ros/ros.key) | sudo apt-key add -
sudo sh -c 'echo "deb [arch=$(dpkg --print-architecture)] [http://packages.ros.org/ros2/ubuntu](http://packages.ros.org/ros2/ubuntu) $(lsb_release -cs) main" > /etc/apt/sources.list.d/ros-latest.list'
sudo apt update
```

**یہ کیا کرتا ہے**:

  - لائن 1: ROS 2 GPG کلید ڈاؤن لوڈ اور شامل کرتی ہے (پیکیج کی صداقت کی تصدیق کرتی ہے)
  - لائن 2: ROS 2 ریپوزٹری کو آپ کے پیکیج کے ذرائع میں شامل کرتی ہے
  - لائن 3: پیکیج لسٹ کو ریفریش کرتی ہے

**متوقع آؤٹ پٹ (Expected output)**:

```text
OK
Hit:1 [http://archive.ubuntu.com/ubuntu](http://archive.ubuntu.com/ubuntu) jammy InRelease
Hit:2 [http://packages.ros.org/ros2/ubuntu](http://packages.ros.org/ros2/ubuntu) jammy InRelease
```

-----

## مرحلہ 3: ROS 2 Humble انسٹال کریں

اب مکمل ROS 2 Humble ڈیسک ٹاپ ڈسٹری بیوشن انسٹال کریں:

```bash
sudo apt install -y ros-humble-desktop
```

یہ ROS 2 core، ڈیویلپمنٹ ٹولز، اور ویژولائزیشن یوٹیلیٹیز انسٹال کرتا ہے۔ انسٹالیشن میں انٹرنیٹ کی رفتار کے لحاظ سے 5-10 منٹ لگتے ہیں۔

**کیا شامل ہے**:

  - `rclpy`: Python ROS 2 کلائنٹ لائبریری
  - `rclcpp`: C++ ROS 2 کلائنٹ لائبریری
  - `ros2`: کمانڈ لائن ٹولز (ros2 run, ros2 topic وغیرہ)
  - `rviz2`: 3D ویژولائزیشن ٹول
  - `gazebo`: فزکس سمیلیٹر (بعد کے ابواب کے لیے)
  - Turtlesim (آپ کا پہلا روبوٹ سمیلیٹر)

**متوقع آؤٹ پٹ (Expected output)**:

```text
Setting up ros-humble-base [version] ...
Setting up ros-humble-desktop [version] ...
Processing triggers for man-db ...
```

**تصدیق (Verification)**: انسٹالیشن کے بعد، چیک کریں کہ ROS 2 انسٹال ہو چکا ہے:

```bash
source /opt/ros/humble/setup.bash
ros2 --version
```

**متوقع آؤٹ پٹ (Expected output)**:

```text
ROS 2 Humble Hawksbill (version 12.x.x)
```

-----

## مرحلہ 4: ماحولیاتی سورسنگ (Environment Sourcing) ترتیب دیں

ہر بار جب آپ نیا ٹرمینل کھولتے ہیں، ROS 2 کمانڈز اس وقت تک کام نہیں کریں گی جب تک کہ آپ سیٹ اپ فائل کو سورس نہ کریں۔ اسے خودکار (automate) بنانے کے لیے، سورسنگ کمانڈ کو اپنی شیل اسٹارٹ اپ فائل میں شامل کریں۔

### آپشن A: Bash کا استعمال (سب سے عام)

سورسنگ کمانڈ کو `.bashrc` میں شامل کریں:

```bash
echo "source /opt/ros/humble/setup.bash" >> ~/.bashrc
source ~/.bashrc
```

### آپشن B: Zsh کا استعمال (macOS ڈیفالٹ)

سورسنگ کمانڈ کو `.zshrc` میں شامل کریں:

```bash
echo "source /opt/ros/humble/setup.zsh" >> ~/.zshrc
source ~/.zshrc
```

**یہ کیا کرتا ہے**: ہر نیا ٹرمینل خود بخود ROS 2 سیٹ اپ کو سورس کرے گا، لہذا آپ کو اسے دستی طور پر ٹائپ کرنے کی ضرورت نہیں ہے۔

**تصدیق (Verification)**: اپنا ٹرمینل بند کریں اور ایک نیا کھولیں۔ پھر چلائیں:

```bash
echo $ROS_DISTRO
```

**متوقع آؤٹ پٹ (Expected output)**:

```text
humble
```

اگر آپ `humble` دیکھتے ہیں، تو سورسنگ صحیح طریقے سے کام کر رہی ہے\!

-----

## تصور 2: ROS ماحولیاتی متغیرات (Environment Variables)

ROS 2 رن ٹائم رویے کو کنفیگر کرنے کے لیے environment variables کا استعمال کرتا ہے۔ تین سب سے اہم یہ ہیں:

### ROS\_DISTRO

```bash
echo $ROS_DISTRO
# Output: humble
```

**مطلب**: آپ ROS 2 Humble استعمال کر رہے ہیں (نہ کہ Rolling، Iron، یا پرانے LTS ورژنز)

### ROS\_DOMAIN\_ID

```bash
echo $ROS_DOMAIN_ID
# Output: (empty by default)
```

**مطلب**: ڈیفالٹ ڈومین ID 0 ہے۔ یہ آپ کے ROS 2 نیٹ ورک کو الگ کرتا ہے۔ اگر آپ ایک ہی نیٹ ورک پر متعدد ROS 2 سسٹم چلاتے ہیں، تو مداخلت کو روکنے کے لیے ہر ایک کو ایک مختلف ڈومین ID (1، 2، 3 وغیرہ) دیں۔

### ROS\_MIDDLEWARE\_IMPL

```bash
echo $ROS_MIDDLEWARE_IMPL
# Output: rmw_cyclonedds_cpp (or cyclonedds, connextdds, etc.)
```

**مطلب**: ROS 2 اپنے مڈل ویئر کے طور پر DDS (Data Distribution Service) کا استعمال کرتا ہے۔ CycloneDDS ہمارے مقاصد کے لیے ڈیفالٹ اور سب سے زیادہ قابل اعتماد ہے۔

-----

## مرحلہ 5: Python ڈیویلپمنٹ ٹولز انسٹال کریں

ROS 2 ڈیویلپمنٹ کے لیے Python 3.10+، pip، اور بلڈ ٹولز کی ضرورت ہوتی ہے۔ انہیں انسٹال کریں:

```bash
sudo apt install -y python3-pip python3-colcon-common-extensions build-essential
```

**اس میں کیا شامل ہے**:

  - `python3-pip`: Python پیکیج مینیجر
  - `colcon`: ROS 2 پیکیجز کے لیے بلڈ سسٹم
  - `build-essential`: کمپائلر ٹول چین (gcc, g++, make)

**تصدیق (Verification)**: Python ورژن چیک کریں:

```bash
python3 --version
# Expected: Python 3.10.x or higher
```

-----

## مرحلہ 6: Turtlesim لانچ کریں (آپ کی پہلی سیمولیشن)

اب دلچسپ حصے کے لیے—Turtlesim لانچ کریں\!

### ٹرمینل 1: Turtlesim Node لانچ کریں

```bash
ros2 run turtlesim turtlesim_node
```

**متوقع آؤٹ پٹ (Expected output)**:

```text
[INFO] [turtlesim]: Starting turtlesim with node name /turtlesim
[INFO] [turtlesim]: Waiting for external commands on topic /turtle1/cmd_vel
```

آپ کو ایک ونڈو نظر آنی چاہیے جس میں نیلا گرڈ اور مرکز میں ایک نارنجی کچھوا ہو:

```text
┌──────────────────────────────────────┐
│                                      │
│                                      │
│              grid background         │
│                                      │
│            turtle in center (🐢)     │
│                                      │
│                                      │
└──────────────────────────────────────┘
```

**اس ٹرمینل کو چلتا رہنے دیں\!** `turtlesim_node` پروسیس کو فعال رہنا چاہیے۔

### ٹرمینل 2: کچھوے کو کنٹرول کریں (Control the Turtle)

ایک **نیا ٹرمینل** کھولیں (ٹرمینل 1 کو چلتا رکھیں) اور کی بورڈ ٹیلی اوپ (teleop) نوڈ استعمال کریں:

```bash
ros2 run turtlesim turtle_teleop_key
```

**متوقع آؤٹ پٹ (Expected output)**:

```text
Reading from keyboard
---------------------------
Moving around:
        w
      a   d
        s

q/z : increase/decrease max speed by 10%
e/c : increase/decrease only linear speed by 10%
r/v : increase/decrease only angular speed by 10%

anything else : stop

q (quit)
```

اب اپنا کی بورڈ استعمال کریں:

  - **w**: آگے بڑھیں
  - **s**: پیچھے جائیں
  - **a**: بائیں مڑیں (گھڑی کی مخالف سمت)
  - **d**: دائیں مڑیں (گھڑی کی سمت)
  - **q**: باہر نکلیں (Quit)

کچھوے کو ادھر ادھر گھمانے کی کوشش کریں۔ آپ کو اسے ونڈو میں حرکت کرتے ہوئے اور ایک لکیر چھوڑتے ہوئے دیکھنا چاہیے\!

-----

## مرحلہ 7: ROS 2 کمیونیکیشن کی تصدیق کریں

دونوں ٹرمینلز کو چلتا رکھیں اور ROS 2 سسٹم کی تصدیق کے لیے ایک **تیسرا ٹرمینل** کھولیں:

### تمام Nodes کی فہرست بنائیں

```bash
ros2 node list
```

**متوقع آؤٹ پٹ (Expected output)**:

```text
/turtlesim
/teleop_turtle
```

دو نوڈس چل رہے ہیں: سمیلیٹر اور ٹیلی اوپ کنٹرولر۔

### تمام Topics کی فہرست بنائیں

```bash
ros2 topic list
```

**متوقع آؤٹ پٹ (Expected output)**:

```text
/parameter_events
/rosout
/turtle1/cmd_vel
/turtle1/color_sensor
/turtle1/pose
```

**مطلب**:

  - `/turtle1/cmd_vel`: جہاں `teleop_turtle` ولاسٹی کمانڈز publish کرتا ہے
  - `/turtle1/pose`: جہاں `turtlesim` کچھوے کی پوزیشن/سمت publish کرتا ہے
  - `/turtle1/color_sensor`: جہاں `turtlesim` کچھوے کے نیچے کا رنگ publish کرتا ہے

### Topic کا ڈیٹا Real-Time میں دیکھیں

ٹرمینل 2 کے ساتھ کچھ کمانڈز publish کریں ('w' دبائیں آگے بڑھنے کے لیے) اور ڈیٹا کو بہتے ہوئے دیکھیں:

```bash
ros2 topic echo /turtle1/cmd_vel
```

**متوقع آؤٹ پٹ** (حرکت کرتے وقت):

```yaml
linear:
  x: 2.0
  y: 0.0
  z: 0.0
angular:
  x: 0.0
  y: 0.0
  z: 0.0
---
```

یہ بھیجی جانے والی ولاسٹی کمانڈ کو ظاہر کرتا ہے۔ ہر `---` الگ کرنے والا (separator) ایک نئے پیغام کی نشاندہی کرتا ہے۔

ایکو (echo) کو روکنے کے لیے Ctrl+C دبائیں۔

-----

## تصور 3: ROS 2 پیکیج اسٹرکچر (پیش نظارہ)

سبق 4 کے لیے، آپ اپنا ROS 2 پیکیج بنائیں گے۔ اسٹرکچر یہ ہے:

```text
my_robot_workspace/
├── src/
│   └── my_first_node/
│       ├── my_first_node/
│       │   └── __init__.py
│       │   └── publisher_node.py
│       ├── setup.py
│       ├── setup.cfg
│       ├── package.xml
│       └── resource/my_first_node
└── install/
└── build/
```

**اہم فائلیں**:

  - `package.xml`: میٹا ڈیٹا (پیکیج کا نام، ورژن، انحصار/dependencies)
  - `setup.py`: Python پیکیج کنفیگریشن
  - `my_first_node/publisher_node.py`: آپ کا اصل نوڈ کوڈ

ہم سبق 4 میں یہ اسٹرکچر بنائیں گے۔ ابھی کے لیے، صرف لے آؤٹ کو سمجھیں۔

-----

## تصور 4: انحصار (Dependencies) کو سمجھنا

ROS 2 کے بہت سے انحصار (dependencies) ہیں۔ اس باب کے لیے سب سے اہم:

| Package | Purpose | When You'll Use It |
|---------|---------|-------------------|
| `rclpy` | Python ROS 2 library | ہر وہ نوڈ جو آپ لکھیں گے |
| `geometry_msgs` | Twist, Pose, Vector3 messages | نقل و حرکت (Movement) کی کمانڈز |
| `std_msgs` | String, Int32, Float64, Bool | سادہ میسج کی اقسام |
| `sensor_msgs` | Image, LaserScan, PointCloud2 | Sensor ڈیٹا |
| `nav_msgs` | Odometry, MapMetaData | نیویگیشن ڈیٹا |

یہ `ros-humble-desktop` میں پہلے سے انسٹال ہیں۔ جب ہم اپنی مرضی کے مطابق پیکیجز بنائیں گے، تو ہم `package.xml` میں انحصار (dependencies) کی وضاحت کریں گے۔

-----

## خود تشخیص چیک لسٹ (Self-Assessment Checklist)

سبق 3 کی طرف بڑھنے سے پہلے، تصدیق کریں:

  - [ ] میں نے ROS 2 Humble کامیابی سے انسٹال کر لیا
  - [ ] `ros2 --version` دکھاتا ہے "ROS 2 Humble"
  - [ ] میں نے Turtlesim لانچ کیا اور کچھوے والی ونڈو دیکھی
  - [ ] میں نے کی بورڈ کمانڈز (w/a/s/d) کے ساتھ کچھوے کو کنٹرول کیا
  - [ ] `ros2 node list` دکھاتا ہے `/turtlesim` اور `/teleop_turtle`
  - [ ] `ros2 topic list` دکھاتا ہے `/turtle1/cmd_vel` اور `/turtle1/pose`
  - [ ] میں سمجھتا ہوں کہ ROS 2 ماحولیاتی متغیرات (ROS\_DISTRO وغیرہ) پر انحصار کرتا ہے
  - [ ] Python 3.10+ انسٹال ہے (`python3 --version`)

اگر کوئی چیک ناکام ہو جائے، تو نیچے ٹربل شوٹنگ سیکشن کا جائزہ لیں۔

-----

## ٹربل شوٹنگ (عام انسٹالیشن کے مسائل)

### خرابی: "ros2: command not found"

**وجہ**: ROS 2 `setup.bash` سورس نہیں کیا گیا ہے۔

**حل**:

```bash
source /opt/ros/humble/setup.bash
ros2 --version
```

اگر یہ کام کرتا ہے، تو سورسنگ کو اپنے `.bashrc` میں شامل کریں (مرحلہ 4 دیکھیں)۔

-----

### خرابی: "E: Could not get lock /var/lib/apt/lists/lock"

**وجہ**: ایک اور apt پروسیس چل رہا ہے (شاید Ubuntu کی خودکار اپڈیٹس)۔

**حل**: 5 منٹ انتظار کریں یا دوبارہ بوٹ (reboot) کریں، پھر دوبارہ کوشش کریں:

```bash
sudo apt update
```

-----

### خرابی: "Unable to locate package ros-humble-desktop"

**وجہ**: ROS 2 ریپوزٹری صحیح طریقے سے شامل نہیں کی گئی (مرحلہ 2)۔

**حل**: مرحلہ 2 کو احتیاط سے دوبارہ چلائیں:

```bash
sudo curl -sSL [https://raw.githubusercontent.com/ros/ros.key](https://raw.githubusercontent.com/ros/ros.key) | sudo apt-key add -
sudo sh -c 'echo "deb [arch=$(dpkg --print-architecture)] [http://packages.ros.org/ros2/ubuntu](http://packages.ros.org/ros2/ubuntu) $(lsb_release -cs) main" > /etc/apt/sources.list.d/ros-latest.list'
sudo apt update
```

پھر دوبارہ انسٹال کریں:

```bash
sudo apt install -y ros-humble-desktop
```

-----

### خرابی: "Turtlesim window won't open" یا "QXcbConnection: Could not connect to display"

**وجہ**: ڈسپلے سرور کا مسئلہ (VMs یا WSL2 میں عام)۔

**حل WSL2 کے لیے**:

```bash
export DISPLAY=$(awk '/nameserver / {print $2; exit}' /etc/resolv.conf):0
```

اسے WSL2 کے لیے اپنے `.bashrc` میں شامل کریں۔

**حل VirtualBox کے لیے**: یقینی بنائیں کہ VM سیٹنگز میں 3D ایکسلریشن فعال ہے اور مناسب ویڈیو میموری (128MB+) استعمال کریں۔

-----

### خرابی: "No module named 'rclpy'" کسی Python اسکرپٹ میں

**وجہ**: اسکرپٹ چلاتے وقت ROS 2 ماحول سورس نہیں کیا گیا۔

**حل**: یا تو:

1.  چلانے سے پہلے ROS 2 کو سورس کریں: `source /opt/ros/humble/setup.bash && python3 script.py`
2.  اپنے اسکرپٹ میں سورسنگ شامل کریں: `import subprocess; subprocess.run(['source', '/opt/ros/humble/setup.bash'], shell=True)`

-----

### خرابی: "ros2 node list" کوئی نوڈ نہیں دکھاتا

**وجہ**: Turtlesim لانچ نہیں کیا گیا تھا، یا یہ کریش ہو گیا۔

**حل**:

1.  ٹرمینل 1 چیک کریں (جہاں آپ نے `turtlesim_node` چلایا تھا)
2.  خرابی کے پیغامات تلاش کریں
3.  Turtlesim دوبارہ لانچ کریں: `ros2 run turtlesim turtlesim_node`

-----

### نیٹ ورک تنہائی (Network Isolation) (ROS\_DOMAIN\_ID)

**مسئلہ**: آپ کے ROS 2 ٹاپکس اسی نیٹ ورک پر دوسرے ROS 2 سسٹم کے ساتھ ٹکرا رہے ہیں۔

**حل**: نوڈس لانچ کرنے سے پہلے ایک منفرد ڈومین ID سیٹ کریں:

```bash
export ROS_DOMAIN_ID=1
ros2 run turtlesim turtlesim_node
```

اس کورس میں `ROS_DOMAIN_ID=0` (ڈیفالٹ) استعمال کریں؛ اسے صرف اس صورت میں تبدیل کریں جب آپ کے پاس متعدد ROS 2 سسٹم چل رہے ہوں۔

-----

### WSL2 کے ساتھ Windows پر سست انسٹالیشن

**وجہ**: ڈسک I/O کی حد یا اینٹی وائرس کی مداخلت۔

**حل**:

1.  WSL ڈائریکٹریز کو اینٹی وائرس اسکیننگ سے خارج کریں
2.  Ubuntu WSL کو مقامی WSL ڈائریکٹری (نہ کہ `/mnt/c`) میں چلائیں: `cd ~`
3.  GUI انسٹالرز کے بجائے `sudo apt install` استعمال کریں

-----

### سیٹ اپ ڈاٹ بیش سورس کرتے وقت "Permission denied"

**وجہ**: فائل کی اجازت (permissions) کا مسئلہ۔

**حل**:

```bash
chmod +x /opt/ros/humble/setup.bash
source /opt/ros/humble/setup.bash
```

-----

## مزید ایکسپلوریشن (اختیاری)

1.  **ROS 2 Installation Docs**: https://docs.ros.org/en/humble/Installation.html
2.  **Turtlesim Documentation**: https://docs.ros.org/en/humble/Tutorials/Turtlesim/Introducing-Turtlesim.html
3.  **ROS 2 Cheat Sheet**: https://ubuntu.com/tutorials/ros-2-humble-cheat-sheet
4.  **Environment Setup Guide**: https://docs.ros.org/en/humble/Tutorials/Configuring-ROS2-Environment.html

-----

## آگے کیا ہے؟

**سبق 3: نوڈس اور کمیونیکیشن پیٹرنز** میں، آپ کریں گے:

  - جانیں کہ نوڈس، ٹاپکس اور میسجز ایک دوسرے سے کیسے تعلق رکھتے ہیں
  - Publish-subscribe بمقابلہ سروس پیٹرنز کو سمجھیں
  - چلتے ہوئے سسٹمز کا معائنہ کرنے کے لیے ROS 2 CLI ٹولز استعمال کریں
  - ROS 2 آرکیٹیکچر کا ایک تصوراتی ماڈل بنائیں

**متوقع وقت**: 60 منٹ

**سبق 3 کے لیے ضروریات**: ROS 2 ماحول کام کر رہا ہو، Turtlesim فعال ہو

-----

## Layer 2: AI Collaboration Prompt

**Claude یا کسی دوسرے AI اسسٹنٹ کے ساتھ کام کرنے والے طلباء کے لیے:**

> "I just installed ROS 2 and launched Turtlesim. Ask Claude: 'What's happening behind the scenes when I press 'w' on my keyboard? How does the keyboard input eventually make the turtle move?'"

**یہ کیوں اہم ہے**: Input → teleop node → turtlesim node سے ڈیٹا کے بہاؤ کو سمجھنا ROS 2 میسج پاسنگ کے لیے بدیہی سمجھ (intuition) پیدا کرتا ہے۔

-----

## تصور شمار اور CEFR توثیق

**کل A2 تصورات متعارف کرائے گئے**: 7

1.  Ubuntu 22.04 اور Linux کی ضروریات
2.  ROS 2 Humble انسٹالیشن اور تصدیق
3.  ماحولیاتی سورسنگ (setup.bash)
4.  ROS ماحولیاتی متغیرات (ROS\_DISTRO, ROS\_DOMAIN\_ID)
5.  Turtlesim سمیلیٹر اور کی بورڈ ٹیلی آپریشن
6.  ROS 2 CLI ٹولز (ros2 run, ros2 node list, ros2 topic list)
7.  ROS 2 پیکیج اسٹرکچر (سبق 4 کے لیے پیش نظارہ)

**CEFR A2 الائنمنٹ**: ✓ مرحلہ وار طریقہ کار سیکھنا، واضح ضروریات، کوئی تجریدی تصورات نہیں

-----

**باب 1، سبق 2 مکمل**

اگلا: سبق 3 — نوڈس اور کمیونیکیشن پیٹرنز (60 منٹ)

</PersonalizedLesson>


